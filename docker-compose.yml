version: '3.8'
services:
  auth-api:
    build: .
    container_name: prosiga-login
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    networks:
      - prosiga-net

networks:
  prosiga-net:
    external:
      name: back-prosiga_default